{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "InstantiateMsg",
  "type": "object",
  "required": [
    "share_addr"
  ],
  "properties": {
    "admin": {
      "type": [
        "string",
        "null"
      ]
    },
    "share_addr": {
      "description": "share_addr is the address of the coin-manager contract, need to send to rewards.",
      "type": "string"
    }
  }
}
